ORYN UNIVERSAL HOME BOOTSTRAP V4
Build: UC-HOME-BOOTSTRAP-V4-20260827-1

FIXES:
- Startup and manual sensorless HOME can run before first exact perimeter calibration
- Current installed machine profile provides 52-unit bootstrap HOME only
- Fresh saved perimeter calibration immediately overrides bootstrap
- Crash HOME sends exactly one relative Y jog; never retries a relative HOME
- Pattern executor/calibration UI/static assets otherwise unchanged from V3 baseline
