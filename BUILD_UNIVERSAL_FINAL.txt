ORYN UNIVERSAL DRIVER PROFILE V8 — ABSOLUTE THETA-RHO STREAM FIX
Build: UC-DRIVER-PROFILE-V8-ABS-THR-20260827-1

CORE FIXES:
- Universal pattern geometry no longer uses raw MPos/WPos as origin
- Explicit G92 logical Theta-Rho work frame at each universal pattern start
- Absolute G90 targets derived directly from saved 360 and perimeter calibration
- Absolute target retries cannot double radial travel
- No per-point Idle wait; planner can blend continuously to remove jerking
- Lost/consumed ok uses live FluidNC status fallback without stopping valid motion
- Machine Profile keeps FluidNC config-tree + GRBL $100/$101/$110/$111/$120/$121 fallback
- Existing UI, HOME, driver profile, calibration controls and static images preserved
